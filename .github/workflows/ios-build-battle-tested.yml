name: iOS Build (Battle-Tested)

on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 35
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📱 Setup Node.js
        uses: actions/setup-node@v4
        with: 
          node-version: 20
          cache: 'npm'

      - name: 🛠️ Global dependencies
        run: npm ci && npm install -g expo-cli eas-cli

      # ---------- 1. generate ios/ (if needed) ----------
      - name: 🍎 Expo prebuild (iOS)
        if: ${{ !hashFiles('ios/Podfile') }}
        run: |
          npx expo prebuild --clean --platform ios \
               --non-interactive --no-install
          echo "✓ expo prebuild finished"

      # ---------- 2. Debug + Guard (CRITICAL) ----------
      - name: 🔍 Debug + Guard Check
        run: |
          echo "=== iOS folder contents ==="
          ls -al ios
          echo "=== Podfile existence check ==="
          test -f ios/Podfile
          echo "✅ Podfile confirmed present"

      # ---------- 3. install pods ----------
      - name: 📦 CocoaPods Install
        run: |
          cd ios
          pod install --repo-update
        env:
          CP_DISABLE_REPO_SYNC: 1   # speed-up

      # ---------- 4. Verify project structure ----------
      - name: ✅ Verify Xcode workspace
        run: |
          echo "=== Workspace verification ==="
          ls -la ios/*.xcworkspace
          echo "=== Available schemes ==="
          cd ios && xcodebuild -list

      # ---------- 5. build unsigned .xcarchive ----------
      - name: 🏗️ Xcode Release build
        run: |
          xcodebuild -workspace ios/PolyglotKey.xcworkspace \
                     -scheme PolyglotKey \
                     -configuration Release \
                     -sdk iphoneos \
                     -archivePath $PWD/build/PolyglotKey.xcarchive \
                     archive CODE_SIGNING_ALLOWED=NO

      # ---------- 6. artifacts ----------
      - name: 📦 Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-archive
          path: |
            build/PolyglotKey.xcarchive
            ios/Podfile
            ios/Podfile.lock
          retention-days: 7

      - name: 📋 Upload debug info on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ios-debug-failure
          path: |
            ios/
            ~/Library/Logs/DiagnosticReports/
          retention-days: 3 